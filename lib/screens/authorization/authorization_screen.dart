import 'package:flutter/material.dart';
import 'package:stom_club/screens/authorization/authorization_screen_body.dart';

class AuthorizationScreenWidget extends StatelessWidget {
  const AuthorizationScreenWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const AuthorizationScreenBodyWidget();
  }
}
